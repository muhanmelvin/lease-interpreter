{
  "name": "lease-interpreter",
  "version": "0.1.0",
  "private": true,
  "description": "The LLM-drafts-human-confirms workflow as a static page: build the extraction prompt, paste the draft back, resolve the flags, see the dollar spread, export lease-terms JSON. The AI reads; it never computes.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "vite build && node scripts/build-gate.mjs",
    "build:single": "cross-env SINGLE_FILE=1 vite build && node scripts/finalize-single.mjs && node scripts/build-gate.mjs dist-single",
    "preview": "vite preview",
    "ci": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "cross-env": "^7.0.3",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.3",
    "vitest": "^2.1.0"
  }
}
